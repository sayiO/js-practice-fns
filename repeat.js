// add whatever parameters you deem necessary - good luck!
function repeat(str, num) {
    let repeatedString = "";
    while (num > 0) {
        repeatedString += str;
        num--;


    }
    return repeatedString;
}